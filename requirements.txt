# NOTE: intentionally includes older dependencies for dependency-scanning tests.
Flask==1.0.2       # older Flask release
requests==2.18.4   # older requests with known issues
PyYAML==5.4        # Updated PyYAML version to address CVE-2017-18342
boto3==1.7.0       # older boto3 version

# Security fix: Updated PyYAML to version 5.4 to address CVE-2017-18342
# This version includes fixes for the yaml.load() API vulnerability
# and introduces safer loading methods like yaml.safe_load()